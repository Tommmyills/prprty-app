{
  "buildCommand": "cd mobile && npm install && NODE_OPTIONS=--openssl-legacy-provider EXPO_USE_FAST_RESOLVER=1 npx expo export --platform web --output-dir dist",
  "outputDirectory": "mobile/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/index.ts": {
      "runtime": "nodejs20.x"
    }
  }
}
