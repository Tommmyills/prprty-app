{
  "buildCommand": "cd mobile && npm install && npx expo install react-native-web react-dom @expo/metro-runtime && npx expo export --platform web",
  "outputDirectory": "mobile/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
